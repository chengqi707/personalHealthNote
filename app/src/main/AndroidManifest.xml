<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 13+ 读取图片权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 12及以下 读取存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- 网络权限，用于AI评估 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ 通知权限（用于用药提醒） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 12+ 精确闹钟权限（用于用药提醒定时） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="33" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalHealthNote">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HealthRecordEditActivity"
            android:parentActivityName=".activity.MainActivity"
            android:exported="false"
            android:label="健康记录" />
        <activity
            android:name=".activity.MedicineReminderEditActivity"
            android:parentActivityName=".activity.MainActivity"
            android:exported="false"
            android:label="用药提醒" />
        <activity
            android:name=".activity.HealthRecordDetailActivity"
            android:parentActivityName=".activity.MainActivity"
            android:exported="false"
            android:label="健康记录详情" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="false"
            android:label="登录" />
        <activity
            android:name=".activity.MedicalRecordAddActivity"
            android:parentActivityName=".activity.MainActivity"
            android:exported="false"
            android:label="新增就医记录" />
        <activity
            android:name=".activity.MedicalRecordDetailActivity"
            android:parentActivityName=".activity.MainActivity"
            android:exported="false"
            android:label="就医记录详情" />
        <activity
            android:name=".activity.MedicalRecordEditActivity"
            android:parentActivityName=".activity.MedicalRecordDetailActivity"
            android:exported="false"
            android:label="编辑就医记录" />
        <activity
            android:name=".activity.ImagePreviewActivity"
            android:exported="false"
            android:label="图片预览"
            android:theme="@style/Theme.PersonalHealthNote.FullScreen" />
    </application>

</manifest>