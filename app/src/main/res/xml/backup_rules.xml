<?xml version="1.0" encoding="utf-8"?>
<!-- Full backup rules for Android 12+ (API 31+).
     No include/exclude rules = default behavior = all app data is backed up.
     This covers: databases, shared preferences, getFilesDir(), getDir() directories, etc. -->
<full-backup-content>
</full-backup-content>
